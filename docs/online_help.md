# Online help

## Run Statistics

The table in this section gathers informations and statistics about the run.

- **Report name**: Name of the report. It is defined using the `--report-name` argument in command line of ToulligQC. If not set the rapport name will contains the report generation date.
- **Experiment group**: The experiment name defined when launching a run. This value is not defined for runs generated with old versions of MinKNOW.
- **Sample ID**: The name of the sample loaded on the flowcell.
- **Run ID**: Unique identifier of the run (a 40 hexadecimal character string).
- **Run date**: The run date.
- **Run duration**: The duration of the run.
- **Flowcell ID**: The serial number of the flowcell.
- **Flowcell product code**: The flowcell product code. This value is not defined for runs generated with old versions of MinKNOW.
- **Flowcell version**: This information comes from the `--flowcell` argument of guppy. No value will be displayed if this argument is not used or no telemetry file provided in ToulligQC input. This value should be the same as the flowcell product code.
- **Kit**: The sequencing kit. This information comes from the `--kit` argument of guppy. No value will be displayed if this argument is not used or no telemetry file provided in ToulligQC input.
- **Sequencing kit**: The sequencing kit stated in the telemetry file.
- **Barcode kit**: The barcode kits. This information comes from the `--barcode_kits` argument of guppy. No value will be displayed if this argument is not used or no telemetry file provided in ToulligQC input.
- **Selected speed (bps)**: Nucleid acid speed through the pores.
- **Sample frequency (Hz)**: Sample frequency for data acquisition.
- **Yield**: The yield of the run in bases.
- **Read count**: The number of reads generated by the run.
- **N50 (bp)**: The N50 value is the length of the shortest read in the set of the longest reads that capture 50% of the run reads.
- **L50**: The L50 value is the number of reads in the set of the longest reads that capture 50% of the run reads.

**Note**: For yield, read count, N50 and L50 computation, reads discarded by Guppy (e.g. calibration strands, zero length reads) are not used for computation but both pass and fails reads are used.

## Device and software

The table in this section gathers informations and parameters about the software used to produce run data.

- **Device type**: The sequencer model (e.g. minion, minion_mk1c).
- **Device ID**: The serial number of the sequencer.
- **Device hostname**: The name of the computer driving the sequencer.
- **Device OS**: The operating system of the computer.
- **Distribution version**: The MinKNOW distribution version used for data acquisition.
- **MinKNOW version**: The MinKNOW application version used for data acquisition.
- **Basecaller name**: The name of the basecaller (e.g. guppy).
- **Basecaller version**: The version of the basecaller.
- **Basecaller analysis**: Type of analysis performed by the basecaller (e.g. 1d_basecalling, 1dsqr_basecalling).
- **Basecalling date**: Date of the basecalling.
- **Model file**: Model file used by the basecaller to perform the analysis.
- **Min qscore threshold**: The mean qscore read threshold for pass reads. Used to be 7 by default but can be defined using the `--min_qscore` argument of Guppy. In the latest versions of Guppy the default threshold if not the same in fast or high accuracy mode.
- **ToulligQC version**: The ToulligQC version.

## Read count histogram

![Read count histogram](help/help01.png)

This histogram shows the counts of reads categories:

- **All reads**: The number of sequenced reads. Empty and calibration reads may not be taken in account (it depends of Guppy version and Guppy parameters). Fast5 files may contains more read entries than the read count output of Guppy. This value must be equal to the number of lines in the sequencing summary file (minus 1 for the header line).
- **Pass reads**: The number of reads that pass Guppy tests (mean Qscore threshold and/or read size).
- **Fail reads**: The number of reads that do not pass Guppy tests.
- **Pass barcoded reads**: The number of Pass reads which barcode is not marked as "unclassified".
- **Fail barcoded reads**: The number of Fail reads which barcode is not marked as "unclassified".

The table displays this graph data in counts or in percent.

**Warning**: Note that if barcoding information is missing for a read, it will be marked as "unclassified" by ToulligQC.

## Distribution of read lengths

![Distribution of read lengths](help/help02a.png)
![Distribution of read lengths](help/help03a.png)

![Distribution of read lengths](help/help02b.png)
![Distribution of read lengths](help/help03b.png)

This graph shows the number of reads against the read length. There are four visualisation modes for this distribution: linear (default) or log scale and reads (default) or bases mode. This graph is centred on the first 99% reads to avoid compression of lower size reads by long artefact reads. You can show the full distribution by clicking the autoscale button. Three vertical line show median, first and third quartiles of read lengths (pass + fail).

## Yield plot through time

![Yield plot through time](help/help04.png)
![Yield plot through time](help/help05.png)

![Yield plot through time](help/help06.png)
![Yield plot through time](help/help07.png)

This graph shows the yield of the sequencer from the beginning to the end of the run. It can display read (default) or base curves in a cumulative (default) or yield way.

## PHRED score distribution

![PHRED score distribution](help/help08.png)
![PHRED score distribution](help/help09.png)

This graph shows the qscore distribution for all, pass and fail reads. A button allows to toggle visualisation from boxplot (default) to violin plots.

The table displays min, max, mean, median, first and third quartile statistics for the three read categories.

## PHRED score density distribution

![PHRED score density distribution](help/help10.png)

This graph shows the distribution of the pass and fails reads against their mean qscore. The two populations must not be overlapping, and the boundary between the two curves is at the qscore threshold defined when launching the basecalling. The slight overlapping between fail and pass distributions is an artefact of curve smoothing. Three additional vertical lines display median, first and third quartiles for pass read distribution.

## Channel occupancy of the flowcell

![Channel occupancy of the flowcell](help/help11a.png)
![Channel occupancy of the flowcell](help/help11b.png)

![Channel occupancy of the flowcell](help/help11c.png)
![Channel occupancy of the flowcell](help/help11d.png)

![Channel occupancy of the flowcell](help/help11e.png)
![Channel occupancy of the flowcell](help/help11f.png)

This plot shows a grid with the read yield of each channel of the flowcell. This graph allows to detect if low throughput of a flowcell is due to a lower number of working channels. The position of a channel in the grid reflect the position of the channel on the flowcell, so spacial bias can be detected using this graph. Geometry of the Flongle, MinION and PromethION flowcells are handled by this graph (the last two screenshots show outputs of this graph with Flongle and PromethION flowcells). There four visualisation modes for this graph, the first three show the number of reads sequenced by each channel for all, pass and fail reads. The last visualisation mode show the percentage of fail reads in each channel.

## Correlation between read length and PHRED score

![Correlation between read length and PHRED score](help/help12.png)

This 2D density plot shows the mean qscore and length for the run reads. A button allow to toggle between pass, fail and all reads visualisation.

## Read length over time

![Read length over time](help/help13.png)

This graph shows the median, first and third quartile distribution of read lengths over time.

## PHRED score over time

![PHRED score over time](help/help14.png)

This graph shows the min, max, median, first and third quartile distribution of the mean qscore reads over time. The green area, include all the pass reads as the lower limit of this area is the mean qscore threshold used by Guppy to classify Pass and Fail reads. If this threshold is not available (missing telemetry file), 7.0 will be using as lower threshold.

## Translocation speed

![Translocation speed](help/help15.png)

This graph shows the median, first and third quartile distribution of translocation speed over time. The translocation speed is the number of bases that pass through a pore per second. The green zone indicates that translocation speed of a read is nominal. The lower threshold is **always** set to 300 b/s, the expected value for DNA in 1D with a R9.4.1 flowcell.

## Pass barcoded reads distribution

![Pass barcoded reads distribution](help/help16.png)
![Pass barcoded reads distribution](help/help17.png)

This graph shows the distribution of the pass reads against their barcodes. This graph allows two visualisation mode: pie chart or histogram. Unclassified reads are reads with a barcode that cannot be assigned. The "other barcode" category gathering all reads that are not unclassified but with a barcode that has not been specified using the `--barcodes` argument of ToulligQC.

The table displays data used to generate the graphs.

## Fail barcoded reads distribution

![Fail barcoded reads distribution](help/help18.png)
![Fail barcoded reads distribution](help/help19.png)

This graph shows the distribution of the fail reads against their barcodes. As this graph is like the previous one, see [Pass barcoded reads distribution help](#pass-barcoded-reads-distribution) for more informations.

## Read size distribution for barcodes

![Read size distribution for barcodes](help/help20.png)

This graph shows for each barcode, two boxplots (one for pass read, the other for fail reads) for the distribution of read lengths. Unclassified reads are reads with a barcode that cannot be assigned. The "other barcode" category gathers all reads that are not unclassified but with a barcode that has not been specified using the `--barcodes` argument of ToulligQC.

## PHRED score distribution for barcodes

![PHRED score distribution for barcodes](help/help21.png)

This graph shows for each barcode, two boxplots (one for pass read, the other for fail reads) for the distribution of mean qscore reads. Unclassified reads are reads with a barcode that cannot be assigned. The "other barcode" category gathers all reads that are not unclassified but with a barcode that has not been specified using the `--barcodes` argument of ToulligQC.
